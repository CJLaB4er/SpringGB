spring:
  application:
    name: spring-testing
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/note_service_test
    username: root
    password: pass
    driver-class-name: com.mysql.jdbc.Driver
